# Testing Rules

All features must include tests.

Test types

unit tests
component tests
integration tests
e2e tests

New code must not break existing tests.

Global tests live in /tests.
Feature specific tests live inside feature folders.
